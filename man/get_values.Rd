% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_fn.R
\name{get_values}
\alias{get_values}
\title{Retrieve latest financial data of symbols}
\usage{
get_values(symbols = c("AAPL", "GOOGL"), .verbose = F)
}
\arguments{
\item{symbols}{Character vector. One or more asset symbols (e.g., \code{c("AAPL", "GOOGL")}).}

\item{.verbose}{Logical. If TRUE, displays verbose output during the fetching process. Default is FALSE.}
}
\value{
A data.frame with 1 row per symbol provided and 21 columns, including:
\describe{
\item{currency}{\code{character} - Trading currency of the asset (e.g., "USD").}
\item{symbol}{ \code{character} - Symbol of the asset, e.g., ticker symbol.}
\item{exchangename}{ \code{character} - Abbreviated exchange place or financial instrument name.}
\item{fullexchangename}{ \code{character} - Full name of the exchange place or financial instrument.}
\item{instrumenttype}{ \code{character} - Type of financial instrument (e.g., "EQUITY").}
\item{firsttradedate}{ \code{POSIXct} - Datetime when the asset was first traded.}
\item{regularmarkettime}{ \code{POSIXct} - Timestamp of the latest regular market quote.}
\item{hasprepostmarketdata}{ \code{logical} - Indicates if pre/post-market data is available.}
\item{gmtoffset}{ \code{integer} - Offset from GMT in seconds.}
\item{timezone}{ \code{character} - Abbreviated timezone name of the market.}
\item{exchangetimezonename}{ \code{character} - Timezone name of the exchange location.}
\item{regularmarketprice}{ \code{numeric}   - Latest regular market trading price.}
\item{fiftytwoweekhigh}{ \code{numeric}   - highest price over a 52-weeks period.}
\item{fiftytwoweeklow}{ \code{numeric}   - lowest price over a  52-weeks period.}
\item{regularmarketdayhigh}{ \code{numeric}   - Highest price during the current market day.}
\item{regularmarketdaylow}{ \code{numeric}   - Lowest price during the current market day.}
\item{regularmarketvolume}{ \code{integer}   - Volume traded during the current market day.}
\item{shortname}{ \code{character} - Shortened or common name of the asset.}
\item{chartpreviousclose}{ \code{numeric}   - Closing price shown in charts.}
\item{previousclose}{\code{numeric}   - Previous official market close price.}
}
}
\description{
Calls an internal API to fetch flat latest (non-historical) metadata for one or more asset symbols.
Returns a data.frame with one row per valid financial symbol, with information such as latest prices and trading context.
}
\details{
Return a \code{data.frame} with additionnal attributes:
\describe{
\item{n.currencies}{\code{integer} - Number of unique currencies in the \code{data.frame}.}
\item{currencies}{\code{character} - A vector of currency symbols in the \code{data.frame} (e.g., \code{"BTC"}, \code{"ETH"}, \code{"USD"}).}
\item{exchange}{\code{character} - A vector of exchange pairs (e.g., \code{"BTC => USD"}). If not applicable, \code{NULL} (no entry).}
\item{date}{\code{Date} - The date when the data was retrieved, set using \code{Sys.Date()}.}
\item{crypto}{\code{logical} - \code{TRUE} if cryptocurrencies are present, otherwise \code{FALSE}.}
}
}
\examples{
get_values(c("AAPL", "GOOGL","SAAB-B.ST"))
}
\references{
Source : https://query2.finance.yahoo.com/v8/finance/chart/
}
